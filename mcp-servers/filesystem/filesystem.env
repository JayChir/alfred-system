# MCP Filesystem Server Configuration
# Port: 7007
# Deployed at: mcp-filesystem.artemsys.ai
# Note: Mounts /opt/mcp/data for file access

PORT=7007
SHELL_CMD="docker run -i --rm -v /opt/mcp/data:/data:rw mcp/filesystem"