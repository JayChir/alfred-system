# MCP Memory Server Configuration
# Port: 7009
# Deployed at: mcp-memory.artemsys.ai
# WARNING: Stores persistent data in Docker volume
# Data persists on server, not locally

PORT=7009
SHELL_CMD="docker run -i --rm -v claude-memory:/app/dist mcp/memory"